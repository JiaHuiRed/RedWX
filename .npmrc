# Suppress npm funding and audit messages during installation
fund=false
audit=false

# Resolve sharp version conflicts
prefer-dedupe=true

# Allow pnpm 11 build scripts for native deps
allowBuilds.esbuild=true
allowBuilds.sharp=true
